import React from 'react';
import { Meta } from '@storybook/react';
import {{> injectPascalCaseName }} from '../{{> injectPascalCaseName }}';

const disableProp = {
  table: {
    disable: true,
  },
};

export default {
  title: 'FILL IN/{{> injectPascalCaseName }}',
  component: {{> injectPascalCaseName }}, 
  argTypes: {
    as: { ...disableProp },
    ref: { ...disableProp },
    theme: { ...disableProp },
    forwardedAs: {
      ...disableProp,
    },
  },
} as Meta;

const Template = (args: any) => (
  <{{> injectPascalCaseName }} {...args} />
);

export const Default = Template.bind({});
// @ts-ignore
Default.args = {};

