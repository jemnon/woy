import React from 'react';
import {{> injectPascalCaseName }} from '../{{> injectPascalCaseName }}';

const disableProp = {
  table: {
    disable: true,
  },
};

const {{> injectPascalCaseName }}Obj = {
  title: 'FILL IN/{{> injectPascalCaseName }}',
  component: {{> injectPascalCaseName }},
  argTypes: {
    as: { ...disableProp },
    ref: { ...disableProp },
    theme: { ...disableProp },
    forwardedAs: {
      ...disableProp,
    },
  },
};

export default {{> injectPascalCaseName }}Obj;

const Template = args => (
  <{{> injectPascalCaseName }} {...args} />
);

export const Default = Template.bind({});
Default.args = {};
