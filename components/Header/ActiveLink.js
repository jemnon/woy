import React from 'react';

const ActiveLink = () => (
  <div
    className="absolute bg-ss-orange w-100 left-0"
    style={{ height: '0.25rem', top: 'calc(100% - 0.25rem)', marginTop: '1px' }}
  />
);

export default ActiveLink;
